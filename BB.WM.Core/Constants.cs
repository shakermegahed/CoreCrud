using System;
using System.Collections.Generic;
using System.Text;

namespace BB.CRM.Core
{
    public static class Constants
    {
        public enum WebSiteStaus
        {
            Active = 1,
            DeActive = 2
        }
        public enum FeatureStatus
        {
            Active = 1,
            DeActive = 2
        }

        public enum EntityType
        {
            Category = 1
        }
    }
}
